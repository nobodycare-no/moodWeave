# 智能体上线须知

> 完整协作协议请阅 `docs/superpowers/AGENTS.md`

## 一句话启动

| 你说 | AI 自动做 |
|------|----------|
| **"继续 MoodWeave"** | `git pull` → 识别身份 → 审查(如需) → 读进度 → 开发 |
| **"今天结束了"** | 更新 status.md → commit → `git push` |

全程你只需要说这两句话，拉取/推送全自动。

## 你的身份

- **在家 →** 我(SOLO) 用 `SOLO <solo@moodweave.dev>`
- **在公司 →** 让 Agent-C 用 `Agent-C <agent-c@moodweave.dev>`
